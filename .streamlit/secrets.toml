# .streamlit/secrets.toml file

# Your Anthropic API key (replace with your actual key, keep the quotes)
anthropic = "YOUR_ANTHROPIC_API_KEY_HERE"

# Note: Make sure there are no spaces before or after the equals sign
# The format should be exactly: KEY = "VALUE"
